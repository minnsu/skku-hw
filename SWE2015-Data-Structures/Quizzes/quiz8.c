#include <stdio.h>
/*int main8() {
	for (int i = 1; i <= numTerms; i++) {
		rowTerms[a[i].col]++;
	}
	startingPos[0] = 1;
	for (int i = 1; i <= numCols; i++) {
		startingPos[i] = startingPos[i - 1] + rowTerms[i - 1];
	}

	for (int i = 0; i <= numCols; i++) { // Data Structures_SWE2015_41 ��μ<student_id>52
		startingPos[i] = 1; // ��� ������ �������� 1�� �ʱ�ȭ
	}
	for (int i = 1; i <= numTerms; i++) { // ���ڰ� �ִ� ����
		// a[i].col�� �������� ������ �־�� ��.
		for (int j = a[i].col + 1; j <= numCols; j++) { // �ش� ������ col���� ū col �κ�
			startingPos[j]++; // �ش� i�� col���� ū col�̸� �������� �� ĭ�� �и�
		}
	}
}*/