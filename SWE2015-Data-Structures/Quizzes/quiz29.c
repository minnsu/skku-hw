#include <stdio.h>
/*
typedef struct node* treePointer;
typedef struct node {
	int data;
	treePointer leftChild, rightChild;
};

int sum = 0;
void visit(treePointer ptr) { // Data Structures_SWE2015_41 ��μ<student_id>52
	if (ptr) { // NULL�� �ƴϸ� 
		sum = sum + ptr->data; // �� ��� ����
		// print��� visit�� inorder, preorder, postorder �ǹ̰� ������
		// sum�̹Ƿ� visit�̶�� �Լ� ���
		visit(ptr->leftChild);
		visit(ptr->rightChild);
	}
	return;
}
int treeAdd(treePointer tree) {
	visit(tree);
	return sum;
}

int main() {

	return 0;
}*/